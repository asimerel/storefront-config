apiVersion: v1
kind: Service
metadata:
  name: redis
  labels:
    app.kubernetes.io/component: redis
    app.kubernetes.io/name: redis
spec:
  type: ClusterIP
  clusterIP: None
  ports:
  - port: 6379
    name: redis
    targetPort: 6379
  selector:
    app.kubernetes.io/name: redis
