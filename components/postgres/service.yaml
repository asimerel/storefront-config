apiVersion: v1
kind: Service
metadata:
  name: postgres
  labels:
    app.kubernetes.io/component: postgres
    app.kubernetes.io/name: postgres
spec:
  type: ClusterIP
  clusterIP: None
  ports:
  - port: 5432
    name: postgres
    targetPort: 5432
  selector:
    app.kubernetes.io/name: postgres
