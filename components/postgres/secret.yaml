# NON-PRODUCTION CREDENTIAL — disposable pilot cluster, private repo.
# Password is intentionally static and in-manifest.
# Do not reuse this pattern for anything that isn't a disposable pilot.
apiVersion: v1
kind: Secret
metadata:
  name: postgres
type: Opaque
stringData:
  POSTGRES_PASSWORD: pilot-local-only
