apiVersion: v1
kind: Service
metadata:
  name: flagd
  labels:
    app.kubernetes.io/component: flagd
    app.kubernetes.io/name: flagd
spec:
  selector:
    opentelemetry.io/name: flagd
  ports:
    - name: grpc
      port: 8013
      targetPort: 8013
    - name: ofrep
      port: 8016
      targetPort: 8016
