apiVersion: v1
kind: Service
metadata:
  name: cart
  labels:
    app.kubernetes.io/tier: backend
    opentelemetry.io/name: cart
    app.kubernetes.io/component: cart
    app.kubernetes.io/name: cart
    app.kubernetes.io/part-of: webshop
# Ingress to this Service is limited to checkout-path callers; see networkpolicy.yaml.
spec:
  type: ClusterIP
  ports:
  - port: 8080
    name: tcp-service
    targetPort: 8080
  selector:
    opentelemetry.io/name: cart
