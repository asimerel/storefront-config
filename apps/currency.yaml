apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: currency
  namespace: argocd
spec:
  project: default
  source:
    repoURL: git@github.com:asimerel/storefront-config.git
    targetRevision: main
    path: components/currency
  destination:
    name: in-cluster
    namespace: shop
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
